version: '3'
services:
  server:
    container_name: app
    restart: always
    build: .
    ports:
      - '8080:8081'
    links:
      - mongo
  mongo:
    container_name: my_database
    image: mongo
    ports:
      - '27017:27017'